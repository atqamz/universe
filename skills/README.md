# Shared Skills

These skills are exported from installed Claude plugins and committed here as a stable, reviewable source for other harnesses.
Run `scripts/sync-claude-plugin-skills` after `claude plugin update` to refresh them intentionally.

Claude plugin updates only modify Claude's plugin cache under `~/.claude/plugins/`.
They do not modify this directory unless the sync script is run.
