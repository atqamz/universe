# Harness Adapters

Adapters expose `skills/` to harness-specific discovery layouts without making plugin caches part of the public interface.

`opencode/skills` is a flattened symlink tree because OpenCode expects `skills/<name>/SKILL.md` directly.
