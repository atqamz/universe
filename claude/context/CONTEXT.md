# Context discipline

- Keep work in chunks under ~100K tokens. Finish one self-contained chunk, then stop, recommend `/compact` (continue) or `/clear` (new task) before next.
- Past ~100K a hook injects reminder — act on it, don't wait to be asked.
- Don't pre-load unneeded context: read the slice you need, not whole file; targeted search over broad dump.
