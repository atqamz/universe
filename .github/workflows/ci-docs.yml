name: Build and check

on:
  pull_request:
    paths:
      - "**.md"
      - "docs/**"
      - ".gitignore"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "docs-only change; flake check not required"
